#include <stdlib.h>

/**
 * main - Print alpha in lower case
 * Return: Always (Succsess)
 */
int main(void)
{
	char lc;

	for (lc == 'a'; lc <= z 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}



